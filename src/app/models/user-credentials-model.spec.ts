import { UserCredentialsModel } from './user-credentials-model';

describe('UserCredentialsModel', () => {
  it('should create an instance', () => {
    expect(new UserCredentialsModel()).toBeTruthy();
  });
});
